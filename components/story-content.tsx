import {sourceEvidence,evidenceLabel} from '@/lib/source-evidence';
import {sourceName} from '@/lib/catalog';
import {articleTags,tagStyle} from '@/lib/tags';
import {ShareStory} from '@/components/share-story';
export function StoryContent({item:i}:{item:any}){const d=i.document;const evidence=d?.evidence||sourceEvidence(i);
return <article className="standalone-story"><p>{articleTags(i).map(t=><span className="colored-tag" key={t} style={tagStyle(t)}>{t}</span>)}</p><h1>{i.translation?.title || i.title}</h1><p>{i.publication||sourceName(i.source_id)} · {i.authors?.length?i.authors.join(', '):'Author not supplied'} · {new Date(i.published_at).toLocaleString('en-GB',{timeZone:'UTC'})} UTC</p><p><a href={i.url} target="_blank" rel="noopener noreferrer">Read full article at source ↗</a></p><p className="muted">{evidenceLabel(evidence.kind)}{evidence.retrievedAt&&<> Source evidence retrieved {new Date(evidence.retrievedAt).toLocaleString('en-GB',{timeZone:'UTC'})} UTC.</>}</p><ShareStory url={`https://news.bittrees.org/story/${i.id}`}/>
{d?<><h2>Summary</h2><p>{d.briefing.overview}</p><h2>Key points</h2><ul>{d.briefing.points.map((p:string,n:number)=><li key={n}>{p}</li>)}</ul><h2>Context and limitations</h2><p>{d.briefing.limitations}</p><p className="muted">Generated by {d.model} on {new Date(d.generatedAt).toLocaleString('en-GB',{timeZone:'UTC'})} UTC. Based on publisher-provided feed evidence; may not cover the full original article. Check the source for details.</p></>:<><h2>Summary in preparation</h2><p>{i.translation?.summary||i.summary||i.excerpt}</p><p>An expanded summary is queued. The original source is available above.</p></>}
{i.cid?<p><a href={`https://ipfs.io/ipfs/${i.cid}`} target="_blank" rel="noopener noreferrer">View archived briefing on IPFS</a><br/><small>{i.cid}</small></p>:<p className="muted">IPFS archive pending.</p>}</article>;}
